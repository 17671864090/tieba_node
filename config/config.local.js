exports.mysql = {
    client: {
        // host
        host: '60.205.178.222',
        // 端口号
        port: '3306',
        // 用户名
        user: '123456',
        // 密码
        password: '123456',
        // 数据库名
        database: 'tbk',
    },
    // 是否加载到 app 上，默认开启
    app: true,
    // 是否加载到 agent 上，默认关闭
    agent: false,
};